# === GO (GOLANG) ===
# Compiled binaries and executables
/app/app
/app/gamevault-backend
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test and coverage files
*.test
*.out
coverage.txt

# Dependency download cache (safe to omit, go.sum already pins versions)
/vendor/

# === DATABASE ===
# Local SQLite files, in case that path is revisited
*.db
*.db-journal

# === ENVIRONMENT VARIABLES (Security) ===
# Real credentials must never be committed
.env
.env.local
.env.*
postgres.env

# === EDITORS AND SYSTEM ===
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp
.DS_Store
Thumbs.db